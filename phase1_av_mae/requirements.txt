# ============================================================
# av-physics-mae — Python Dependencies
# Cross-Modal Masked Autoencoder for Deepfake Detection
# ============================================================
# Install: pip install -r requirements.txt
# Python  : 3.9+
# CUDA    : 11.8+ recommended (CPU fallback supported)
# ============================================================

# ── Core Deep Learning ──────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# ── Vision Transformer Building Blocks ──────────────────────
timm>=0.9.12                  # ViT blocks, patch embeddings

# ── Tensor Manipulation ─────────────────────────────────────
einops>=0.7.0                 # Elegant rearrange / reduce ops for patching

# ── Audio Processing ────────────────────────────────────────
librosa>=0.10.1               # Mel-spectrogram, audio utilities
soundfile>=0.12.1             # Audio I/O backend
audioread>=3.0.1              # Fallback audio decoding

# ── Video Decoding ──────────────────────────────────────────
av>=12.0.0                    # PyAV — fast, frame-accurate video decoding
decord>=0.6.0                 # Alternative GPU-accelerated video reader

# ── Configuration Management ────────────────────────────────
omegaconf>=2.3.0              # Structured YAML configs with dot-access
hydra-core>=1.3.2             # Optional: advanced config composition

# ── Experiment Tracking ─────────────────────────────────────
wandb>=0.16.0                 # Weights & Biases — loss curves, media logging
tensorboard>=2.14.0           # Alternative / fallback logger

# ── Scientific Computing ────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0

# ── Visualization ───────────────────────────────────────────
matplotlib>=3.7.0
Pillow>=10.0.0
opencv-python>=4.8.0          # Frame extraction, patch visualization

# ── Utilities ───────────────────────────────────────────────
tqdm>=4.66.0                  # Progress bars
rich>=13.6.0                  # Beautiful terminal output
python-dotenv>=1.0.0          # Load .env files for API keys / paths

# ── Testing ─────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0

# ── Code Quality (optional but recommended) ─────────────────
# black>=23.0.0
# isort>=5.12.0
# flake8>=6.1.0
